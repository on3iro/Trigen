import styled from 'styled-components';
import { NavLink } from 'react-router-dom';

import buttonStyles from './buttonStyles';

const StyledNavLink = styled(NavLink)`${buttonStyles}`;

export default StyledNavLink;
